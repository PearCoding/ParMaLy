%token NUM;
%start in;

in:	prefix
	;

prefix:		literal add_expr
			;

add_expr:	mul_expr
			| '+' prefix
			| '-' prefix
			;
			
mul_expr:	'*' prefix
			| '/' prefix
			| '%' prefix
			| '**' prefix
			;
			
literal:	NUM
			;